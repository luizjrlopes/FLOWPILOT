"use client";import {useEffect,useState} from "react";import Link from "next/link";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";import type {Workflow} from "@/lib/types";import {getSession} from "@/lib/session";export default function Workflows(){const [items,setItems]=useState<Workflow[]>([]);useEffect(()=>{api<Workflow[]>("/workflows").then(setItems)},[]);const admin=getSession()?.user.role==='admin';return <><PageHead eyebrow="Workflow catalog" title="Workflows" description="Definições YAML versionadas e inspecionáveis; sem editor visual complexo.">{admin&&<Link className="btn primary" href="/workflows/new">Novo workflow</Link>}</PageHead><div className="tableCard"><table><thead><tr><th>Workflow</th><th>Versão</th><th>Trigger</th><th>Estado</th><th>Execuções</th></tr></thead><tbody>{items.map(w=><tr key={w.id}><td><Link href={`/workflows/${w.id}`}><b>{w.name}</b><small>{w.id}</small></Link></td><td>{w.version}</td><td>{w.trigger}</td><td><span className="badge">{w.status}</span></td><td>{w.executions}</td></tr>)}</tbody></table></div></>}
