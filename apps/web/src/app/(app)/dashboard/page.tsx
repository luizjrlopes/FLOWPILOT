"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";export default function Dashboard(){const [d,setD]=useState<any>(null);useEffect(()=>{api("/dashboard").then(setD)},[]);return <><PageHead eyebrow="Automation control plane" title="Visão geral" description="Execuções determinísticas, IA estruturada e decisões humanas no mesmo histórico."/><div className="metrics"><div className="metric"><span>Execuções</span><b>{d?.executions??'—'}</b></div><div className="metric"><span>Aguardando aprovação</span><b>{d?.waiting_approval??'—'}</b></div><div className="metric"><span>Concluídas</span><b>{d?.completed??'—'}</b></div><div className="metric"><span>DLQ</span><b>{d?.dlq??'—'}</b></div></div><div className="card"><h3>Princípio operacional</h3><p>IA classifica e extrai dados estruturados. Aprovações críticas continuam humanas. Retries e DLQ ficam evidentes e auditáveis.</p></div></>}
