"use client";import {useEffect,useState} from "react";import Link from "next/link";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";export default function Approvals(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any[]>("/approvals").then(setItems)},[]);return <><PageHead eyebrow="Human gate" title="Aprovações" description="O workflow fica bloqueado até uma decisão humana autorizada."/><div className="cards">{items.length?items.map(x=><div className="card" key={x.id}><span className="badge warn">DECISÃO PENDENTE</span><h3>{x.id}</h3><p>{x.input.supplier_name}</p><p>Risco: {x.ai?.risk?.risk||'—'} · confiança {Math.round((x.ai?.risk?.score||0)*100)}%</p><Link className="btn" href={`/executions/${x.id}`}>Revisar execução</Link></div>):<div className="card">Nenhuma aprovação pendente.</div>}</div></>}
