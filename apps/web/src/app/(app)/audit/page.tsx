"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";export default function Audit(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{api<any[]>("/audit").then(setItems)},[]);return <><PageHead eyebrow="Evidence trail" title="Auditoria" description="Trigger, schemas, decisões humanas, retries, conectores e estados em eventos distintos."/><div className="card">{items.map(a=><div className="audit" key={a.id}><small>{new Date(a.at).toLocaleString('pt-BR')}</small><b>{a.actor}</b><section><strong>{a.action}</strong><p>{a.details}</p></section><span className="badge">{a.entity_id}</span></div>)}</div></>}
