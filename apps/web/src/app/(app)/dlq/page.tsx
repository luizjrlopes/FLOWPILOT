"use client";import {useEffect,useState} from "react";import {api} from "@/lib/api";import PageHead from "@/components/PageHead";import type {Run} from "@/lib/types";import {getSession} from "@/lib/session";export default function Dlq(){const [items,setItems]=useState<Run[]>([]);const load=()=>api<Run[]>("/executions").then(x=>setItems(x.filter(r=>r.state==='DLQ')));useEffect(()=>{load()},[]);const admin=getSession()?.user.role==='admin';async function retry(id:string){await api(`/executions/${id}/reprocess`,{method:'POST'});load()}return <><PageHead eyebrow="Failure recovery" title="Dead-letter queue" description="Retries esgotados permanecem inspecionáveis e reprocessáveis sem alterar a execução original."/><div className="tableCard"><table><thead><tr><th>Execução</th><th>Workflow</th><th>Falha</th><th>Tentativas</th><th>Ação</th></tr></thead><tbody>{items.map(r=><tr key={r.id}><td>{r.id}</td><td>{r.workflow_id}</td><td>{r.error}</td><td>{r.retry}/3</td><td>{admin?<button className="btn" onClick={()=>retry(r.id)}>Reprocessar</button>:<span>Requer Admin</span>}</td></tr>)}</tbody></table></div></>}
